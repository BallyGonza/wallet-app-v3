import 'package:flutter/material.dart';

class LightThemeColors {
// Light Theme Colors (Existing)
  static const Color backgroundColor = Color.fromARGB(255, 255, 255, 255);
  static const Color primaryColor = Color(0xffCDE8E5);
  static const Color secondaryColor = Color(0xffEEF7FF);
  static const Color accentColor = Color(0xff7AB2B2);
}

class DarkThemeColors {
// Dark Theme Colors
  static const Color backgroundColor = Color(0xff121212);
  static const Color primaryColor = Color(0xff1E3A4C); // Deep blue-grey
  static const Color secondaryColor =
      Color(0xff2C4857); // Slightly lighter than primary
  static const Color accentColor =
      Color(0xff4FBFA3); // Brighter, more saturated version of accent
}
